#!/usr/bin/env node
/**
 * @deprecated Offline SQLite schema extraction removed in Architecture v2.1 Phase 4.
 * Canonical schema lives in electron/schema.sql (frozen).
 */
console.error('electron:extract-schema is deprecated. Schema is maintained in electron/schema.sql.');
process.exit(1);
