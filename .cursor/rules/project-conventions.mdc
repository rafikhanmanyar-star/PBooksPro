---
description: Core project conventions for PBooks Pro
alwaysApply: true
---

# PBooks Pro – Project Conventions

## Tech Stack
- **Frontend**: React 18, TypeScript, Vite, Tailwind CSS
- **Backend**: Express, PostgreSQL (Desktop Edition, Cloud Edition, staging, production)
- **Build**: `@/*` path alias maps to project root

## Imports
- Prefer relative imports (`../../services/`, `../../hooks/`) for consistency with existing codebase
- Path alias `@/*` is available in tsconfig; use when refactoring deep paths

## Multi-Tenant
- All tenant-scoped data uses `tenant_id`; v2 repositories extend `TenantRepository`
- `authMiddleware` sets `req.tenantId`, `req.userId` on authenticated routes
- See `doc/ARCHITECTURE_V2_AGENT_RULES.md` for agent compliance; `doc/ARCHITECTURE.md` for full guide

## Environments
- Staging (`staging` branch, port 3001) and production (`main`, port 3000) are separate
- Migrations in `database/migrations/`; see `commands.mdc` for release workflow
