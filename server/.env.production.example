# Production database URL (Render external URL for pbookspro-api).
# Copy to .env.production and fill in. Use only for seed-demo:production.
# DATABASE_URL=postgresql://user:password@dpg-xxxxx-a.region-postgres.render.com:5432/dbname?sslmode=require

DATABASE_URL=
