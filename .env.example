# API base URL (used at build time for Vite/Electron).
# The app uses this to talk to the PBooks Pro API server.

# Local development (default if not set)
VITE_API_URL=http://localhost:3000/api

# Staging (for web or Electron staging builds)
# VITE_API_URL=https://pbookspro-api-staging.onrender.com/api

# Production (for Electron production builds – or use api.pbookspro.com)
# VITE_API_URL=https://api.pbookspro.com/api

# Optional: WebSocket server (defaults to same host as API, no /api path)
VITE_WS_URL=https://pbookspro-api-staging.onrender.com
