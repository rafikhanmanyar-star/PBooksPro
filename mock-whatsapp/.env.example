# Mock WhatsApp server – optional env (defaults work without this file)

# Port (default: 9999)
MOCK_WHATSAPP_PORT=9999

# API version path to mimic Meta (default: v21.0)
MOCK_WHATSAPP_API_VERSION=v21.0

# Phone number ID – must match what you set in main app WhatsApp config (default: MOCK_PHONE_NUMBER_ID)
MOCK_WHATSAPP_PHONE_NUMBER_ID=MOCK_PHONE_NUMBER_ID

# Display phone number in webhook payloads (default: +15550000000)
MOCK_WHATSAPP_DISPLAY_NUMBER=+15550000000

# Verify token – use same value in main app when configuring webhook (default: mock-verify-token)
MOCK_WHATSAPP_VERIFY_TOKEN=mock-verify-token

# Main app webhook URL – we POST simulated incoming messages and status updates here
# Example: https://your-app.com/api/whatsapp/webhook
# For local: use ngrok or similar and set e.g. https://abc123.ngrok.io/api/whatsapp/webhook
MOCK_WHATSAPP_WEBHOOK_URL=
