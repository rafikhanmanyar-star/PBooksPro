import React from 'react';
import WorkflowDashboard from './workflow/WorkflowDashboard';

const TaskWorkflowPage: React.FC = () => {
    return (
        <WorkflowDashboard onNavigate={() => { }} />
    );
};

export default TaskWorkflowPage;
