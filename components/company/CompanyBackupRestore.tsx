/**
 * Company Backup & Restore (local SQLite / companyBridge) — not used in API mode.
 */
import React from 'react';

const CompanyBackupRestore: React.FC = () => {
  return null;
};

export default CompanyBackupRestore;
